import React from "react";

export const AuthProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  // Implementa qui la logica reale. 
  return <>{children}</>;
};

export default AuthProvider;
